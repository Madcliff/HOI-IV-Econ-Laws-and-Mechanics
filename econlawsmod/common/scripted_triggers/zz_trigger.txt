z_has_resource = { 
	or = { 
		has_resources_amount = {
			resource = steel
			amount > 1
		}
		has_resources_amount = {
			resource = chromium
			amount > 1
		}
		has_resources_amount = {
			resource = oil
			amount > 1
		}
		has_resources_amount = {
			resource = aluminium
			amount > 1
		}
		has_resources_amount = {
			resource = tungsten
			amount > 1
		}
		has_resources_amount = {
			resource = rubber
			amount > 1
		}
	}
}